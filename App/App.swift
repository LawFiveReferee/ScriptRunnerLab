import SwiftUI

@main
struct ScriptRunnerLabApp: App {
  var body: some Scene {
    WindowGroup {
      ContentView()
        .frame(minWidth: 760, minHeight: 560)
    }
    .defaultSize(width: 900, height: 680)
  }
}
